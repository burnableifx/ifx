[package]
name = "ifx-example-linode-web"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
anyhow = "1"
ifx-program = { path = "../../crates/ifx-program", default-features = false, features = ["host", "linode"] }
