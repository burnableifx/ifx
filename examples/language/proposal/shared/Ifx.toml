[language]
edition = "0.2-draft"

# Proposed edition selector and source syntax; this directory can be its own module repository.
[package]
name = "shared_web"
version = "0.2.0"

[modules]
web = "src/web.ifx"
