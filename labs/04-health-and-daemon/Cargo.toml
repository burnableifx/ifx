[package]
name = "ifx-lab-04"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
ifx-program = { path = "../../crates/ifx-program", default-features = false, features = ["host"] }
